spring:
  cloud:
    config:
      discovery:
        enabled: true
        service-id: configserver
  application:
    name: config-client

server:
  port: 8088

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8762/eureka/,http://localhost:8761/eureka/