server:
  port: 8087

spring:
  cloud:
    config:
      server:
        git:
          uri:
            https://github.com/chenbin911029/springCloudConfig

  application:
    name:
      configserver

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8762/eureka/,http://localhost:8761/eureka/